services:
  mysql:
    image: mysql:8.0
    container_name: product-service
    ports:
      - 3308:3306
    environment:
      MYSQL_ROOT_PASSWORD: Wandera12

    volumes:
      - ./mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./docker/mysql/data:/var/lib/mysql